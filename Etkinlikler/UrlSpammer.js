const { GuildMember, MessageEmbed,Client} = require("discord.js");
const request = require('request')
const ms = require('ms');
module.exports = {
    hazır: "ready",
    altyapı: "true",
    Isim: "Test Event",
    /**
     * @param {Client} client
     */
    catcher: function (client) {
      // setInterval(() => {
      // 
      // }, 10000)
    },
     /**
     * @param {client} ready
     */
    urlspammer: async function () {

        },
  };